# 国际化 (i18n) 全面改造与自动化验证计划

## 1. 目标
针对项目所有页面及其子页面进行国际化改造，消除漏翻译现象（主要针对硬编码的文本），并建立自动化验证机制，避免手工测试。

## 2. 现状分析
- **技术栈**：项目使用自定义的 `I18nContext` 和 `useTranslation` 钩子，翻译文件位于 `i18n/locales/en` 和 `i18n/locales/zh-CN`。
- **当前问题**：虽然已有 i18n 框架，但代码中仍存在大量未被 `t()` 函数包裹的硬编码字符串（主要是英文），导致切换语言时这些内容无法翻译。
- **现有工具**：项目中已存在 `tests/e2e/i18n/i18n-e2e.spec.ts` 等测试脚本，但可能覆盖不全或未针对源代码进行静态分析。

## 3. 自动化验证方案
为了不依赖手工测试，我们将采用 **静态代码分析** + **自动化 E2E 测试** 相结合的方案。

### 3.1 开发静态扫描工具 (`scripts/scan-i18n.ts`)
编写一个基于 TypeScript Compiler API (AST) 的扫描脚本，能够：
1.  **扫描范围**：`apps/electron/src/renderer` 下的所有 `.tsx` 和 `.ts` 文件。
2.  **识别目标**：
    -   JSX 文本节点（例如 `<div>Settings</div>` 中的 "Settings"）。
    -   特定 JSX 属性（例如 `placeholder="Search"`, `title="Close"`, `aria-label`）。
    -   未被 `t()` 包裹的字符串字面量（在逻辑代码中）。
3.  **排除规则**：
    -   已被 `t(...)` 包裹的内容。
    -   `import` 语句、类型定义、键名等非 UI 内容。
    -   纯数字、符号或无意义的字符串。
4.  **输出**：生成一份详细的报告 `i18n-scan-report.json`，列出每个文件中的硬编码字符串位置。

### 3.2 利用现有 E2E 测试
优化并运行现有的 Playwright 测试 (`tests/e2e/i18n/i18n-e2e.spec.ts`)，确保应用运行时的可见文本都已通过 i18n 处理。

## 4. 改造实施流程 (迭代执行)
我们将按模块划分任务，每个模块执行以下步骤：
1.  **扫描**：运行扫描工具，锁定该模块下的硬编码字符串。
2.  **提取与替换**：
    -   为每个硬编码字符串定义语义化的 Key（例如 `settings.general.title`）。
    -   在代码中替换为 `t('key')`。
3.  **翻译更新**：
    -   在 `i18n/locales/en` 中添加 Key 和原英文文本。
    -   在 `i18n/locales/zh-CN` 中添加 Key 和对应的中文翻译。
4.  **验证**：再次运行扫描工具，确认该模块已“清零”。

## 5. 任务分解
根据项目结构，将改造任务分解为以下子任务：

-   **任务 1：基础设施与公共组件**
    -   完善扫描脚本 `scripts/scan-i18n.ts`。
    -   改造 `components/` 目录下的通用 UI 组件。
    -   改造 `layouts/` (Sidebar, Header 等全局布局)。

-   **任务 2：设置页面 (Settings)**
    -   改造 `pages/settings/` 下的所有子页面（General, Models, About 等）。

-   **任务 3：聊天核心 (Chat)**
    -   改造 `pages/chat/` 相关页面。
    -   改造聊天气泡、输入框、工具栏等组件。

-   **任务 4：其他页面**
    -   改造 `pages/home/` 或 `Welcome` 页面。
    -   改造 `pages/login/` (如果有)。
    -   处理 `contexts/` 和 `hooks/` 中的提示信息。

## 6. 最终交付物
1.  **代码变更**：所有硬编码字符串被替换为 i18n 调用。
2.  **翻译文件**：更新后的 `en` 和 `zh-CN` JSON 文件。
3.  **验证报告**：
    -   静态扫描报告（证明代码中无残留）。
    -   E2E 测试报告（证明运行时无漏翻）。
4.  **总结报告**：记录改造过程、遇到的问题及后续维护建议。

## 7. 执行顺序
1.  编写并验证扫描脚本。
2.  执行任务 1 (公共组件)。
3.  执行任务 2 (Settings)。
4.  执行任务 3 (Chat)。
5.  执行任务 4 (其他)。
6.  全局回归测试与报告生成。
